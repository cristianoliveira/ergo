name: CI
on: [push]
jobs:
  tests:
    name: Quick checks
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-go@v3
      with:
        go-version: '1.10.1' # The Go version to download (if necessary) and use.
    - run: make test
